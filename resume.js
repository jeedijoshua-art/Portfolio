/**
 * Resume Application Logic
 * Initializes animations and handles the interactive custom cursor.
 */

// Initialize AOS (Animate On Scroll)
AOS.init({
    duration: 1000,
    once: true,
    offset: 100,
    easing: 'ease-out-cubic'
});

// Custom Cursor Logic - REMOVED AS PER USER REQUEST

